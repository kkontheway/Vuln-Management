# Microsoft Entra ID (Azure AD) 配置
TENANT_ID=xxx
APP_ID=xxx
APP_SECRET=Rxxx
# 选择最近的API端点: us, eu, uk, au, swa, ina，或不填使用全球端点
REGION_ENDPOINT=us.api.security.microsoft.com

# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=vulndb
DB_USER=vuln_app
DB_PASSWORD=vuln_app_password

# MySQL Docker容器 (用于 `docker run mysql:8.0`)
MYSQL_ROOT_PASSWORD=change-this-root-password
